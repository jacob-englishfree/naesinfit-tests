# 내신핏 테스트 제작 SOP (Standard Operating Procedure)

> **이 문서는 절대 규칙이다. 어떤 Claude 세션이든 테스트 출제 요청을 받으면 반드시 이 순서를 따른다.**
> 우회, 생략, "나중에 하겠다" 절대 금지.
> 2026-03-27 확정

---

## 전체 흐름

```
원문 확보 → 출제(Generator) → 구조검증 → 블라인드풀이(Solver) → 정답대조(Verifier)
→ 적대적공격(Breaker) → 자동검증 → 증적리포트 → jacob 확인 → 배포
```

---

## STEP 0: 원문 확보

1. 교과서/모의고사/수능특강 원문 텍스트를 정확히 확보
2. PDF에서 추출한 경우 → **오탈자 없는지 원본과 글자 단위 대조**
3. `fullPassage` 변수에 저장 — 이후 모든 단계에서 이 원문을 기준으로 함
4. 원문이 부정확하면 이후 모든 과정이 무의미 → **원문 정확성이 최우선**

---

## STEP 1: 출제 (Generator)

`출제_가이드라인.md`를 **반드시 읽고** 아래 규격을 지켜 JSON 생성:

### 필수 규격
- 문항 수: 단어 20 / 워크북 20 / 퀴즈 20
- 배점: 쉬움5×4 + 보통10×5 + 어려움5×6 = 정확히 100점
- 선지: 4지선다 (T/F만 2지선다)
- 유형: 가이드라인의 유형 목록 + 배치 순서 준수
- passage 마킹: 빈칸형→____, 어법→①②③④, 부적절어휘→<u>4개

### 절대 금지
- 정답이 지문에 그대로 노출
- 더미 선지 (—, None 등)
- 5지선다 (4지선다로 통일)
- 같은 정답 번호 3연속
- 원문 변조 (한 글자라도)

---

## STEP 2: 구조 검증

```bash
node validate/validate.js [파일경로]
```

- 60개 체크포인트 자동 검증
- S/A급 에러 → auto-fix 자동 수정 (최대 3회 루프)
- 3회 후에도 FAIL → STEP 1로 돌아가서 해당 문항 재출제
- **PASS 없이 다음 단계 진행 절대 금지**

---

## STEP 3: 블라인드 풀이 (Solver)

**이 단계가 핵심. 대충 하면 오류 있는 테스트가 배포됨.**

### 방법
1. JSON에서 `ans`, `wa` 필드를 **제거한 상태**로 문제를 읽는다
2. 지문(passage) + 발문(stem) + 선지(ch)만 보고 **직접 풀이**한다
3. 20문항 전부 풀이 + 근거를 적는다

### 출력 형식 (필수)
```
=== 블라인드 풀이 ===

Q1 (빈칸 어휘 완성, 보통/5점)
  지문: The researcher tried to __________ the results...
  선지: ① replicate ② generate ③ eliminate ④ distribute
  풀이: 연구자가 결과를 복제하려 했으므로...
  내 답: ①

Q2 (어법, 어려움/6점)
  지문: The number of students who ①has ②been ③studying...
  풀이: 주어 "The number"는 단수 취급이므로...
  내 답: ④

... Q20까지 전부
```

### 절대 금지
- 정답(ans)을 미리 보고 풀이하는 것
- "전부 맞았습니다"라고만 말하고 풀이 과정을 생략하는 것
- 20문항 중 일부만 풀고 나머지는 "동일한 방식"이라며 넘기는 것

---

## STEP 4: 정답 대조 (Verifier)

1. Solver의 답 vs Generator의 정답(ans/wa)을 비교
2. **일치/불일치 표로 출력**

```
=== 정답 대조 ===

Q1: 내 답 ① vs 정답 ① → ✅
Q2: 내 답 ④ vs 정답 ① → ❌ 불일치!
Q3: 내 답 ③ vs 정답 ③ → ✅
... Q20까지

결과: 18/20 일치, 2건 불일치
```

3. **불일치 발생 시**:
   - 해당 문항의 정답이 진짜 맞는지 재검토
   - 출제 정답이 틀리면 → 정답 수정
   - 내(Solver) 풀이가 틀리면 → 문제 난이도/모호성 확인
   - 어느 쪽이든 모호하면 → **해당 문항 폐기 + 재출제 → STEP 3부터 다시**

---

## STEP 5: 적대적 공격 (Breaker)

**"이 테스트에서 오류를 찾아라"는 역할로 전환**

### 체크리스트
- [ ] 정답이 2개 이상 가능한 문항이 있는가?
- [ ] 선지가 너무 뻔한 문항이 있는가? (정답 빼고 다 말도 안 됨)
- [ ] 정답이 지문에 노출되는 문항이 있는가?
- [ ] 빈칸만 보고 답을 맞출 수 없는 문항이 있는가? (빈칸이 너무 넓어서)
- [ ] 어법 문제에서 문법 포인트가 실제로 존재하는가?
- [ ] 서술형 정답이 여러 표현으로 가능한데 accept에 빠진 것은?
- [ ] 같은 난이도인데 체감 난이도가 크게 다른 문항이 있는가?
- [ ] 학생이 실제로 이 문제를 시간 내에 풀 수 있는가?

### 출력 형식
```
=== 적대적 공격 결과 ===

Q5: ⚠️ 선지 ②③④가 너무 뻔함. ②는 반의어, ③④는 무관한 단어.
  → 오답 선지 품질 개선 필요

Q12: ⚠️ 정답이 ①②③ 모두 가능해 보임.
  → 발문을 더 구체적으로 수정하거나 문항 폐기

Q18: ✅ 문제 없음

... 20문항 전부 체크
```

### 발견된 문제 처리
- 선지 품질 → 오답 선지 교체
- 정답 모호 → 문항 폐기 + 재출제 → STEP 3부터 다시
- 노출 문제 → passage 수정
- **수정 후 반드시 STEP 3(블라인드 풀이)부터 재실행**

---

## STEP 6: 자동 검증

```bash
node validate/validate.js [파일]           # ① 구조 60체크
node validate/validate-fulltext.js [파일]  # ② 원문 대조
node validate/validate-scoring.js [파일]   # ③ 채점 정확도
```

- 3개 전부 PASS 필수
- FAIL → 해당 항목 수정 → 재실행
- **모든 검증 PASS 없이 배포 절대 금지**

---

## STEP 7: 증적 리포트

**아래 형식으로 리포트를 출력해야 배포 가능. 리포트 없으면 배포 차단.**

```
══════════════════════════════════════════
  테스트 검증 리포트
  파일: data/모의고사/고1/3월/18번/단어.json
  생성일: 2026-03-27
══════════════════════════════════════════

[STEP 2] 구조 검증: ✅ PASS (0 errors, 3 warnings)
[STEP 3] 블라인드 풀이: 20/20 풀이 완료
[STEP 4] 정답 대조: 20/20 일치 ✅
  (불일치 0건)
[STEP 5] 적대적 공격: 이슈 2건 발견 → 수정 완료
  - Q5: 오답 선지 교체
  - Q12: 발문 수정
[STEP 6] 자동 검증:
  - validate: ✅ PASS
  - fulltext: ✅ PASS
  - scoring: ✅ PASS

최종 판정: ✅ 배포 가능
══════════════════════════════════════════
```

---

## STEP 8: 배포

리포트 출력 후 jacob에게 보고. jacob이 "배포해"라고 하면:

```bash
node scripts/deploy.js [파일경로]
```

- HTML 빌드 + 테스트앱(Vercel) 배포 + Supabase DB 등록
- 배포 후 실제 URL 접속하여 화면 확인
- **배포 완료 보고 시 실제 URL 포함 필수**

---

## 절대 규칙

1. **STEP 순서 건너뛰기 금지** — 3(블라인드 풀이) 없이 배포 불가
2. **"전부 맞았습니다" 한 줄 보고 금지** — 20문항 풀이 증적 필수
3. **정답 보고 풀이하기 금지** — ans/wa 제거 후 풀이
4. **불일치 무시 금지** — 1건이라도 불일치 → 해결 후 진행
5. **validate FAIL 무시 금지** — FAIL인 상태로 배포 절대 불가
6. **이전 세션 결과 맹신 금지** — 새 세션에서는 재검증 필수
7. **"나중에 고치겠다" 금지** — 지금 고치고 PASS받고 배포

---

## 이 SOP를 따르면

- 정답이 틀린 문제 → STEP 3~4에서 잡힘
- 선지가 뻔한 문제 → STEP 5에서 잡힘
- 구조적 오류 → STEP 2에서 잡힘
- 원문 변조 → STEP 6에서 잡힘
- 대충 넘기기 → 증적 리포트 없으면 배포 차단

**100% 균일한 품질의 테스트가 자동으로 생산됨.**
